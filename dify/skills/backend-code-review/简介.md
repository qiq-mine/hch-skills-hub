# Dify 后端代码审查 (Backend Code Review)

面向生产级 Python / Flask / SQLAlchemy 后端架构的精准代码审查技能。
涵盖四个维度的规则包：分层架构边界（Controller/Service/Domain/Libs）、数据模型与迁移设计（tenant_id 隔离、无 property 跨表查询、PG/MySQL 兼容）、Repository 存储库抽象复用、以及 SQLAlchemy 显式事务控制与并发锁防护（乐观锁、分布式锁、行级锁）。
严格输出 P0~P3 分级审查结论与落地修复建议。
